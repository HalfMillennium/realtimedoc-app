// Define the enum at the top of your file or in a separate file
export enum SegmentMenuOptions {
  HowItWorks = 'How It Works',
  AnyQuestions = 'Any Questions?',
}

export const allSegmentMenuOptions = Object.values(SegmentMenuOptions);
